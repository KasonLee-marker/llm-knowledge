# Function Calling（函数调用）

- 概念：通过将模型输出映射到结构化函数调用（name + arguments）来安全执行外部操作。
- 优点：
  - 明确意图（比纯文本解析更可靠）
  - 更易于验证与重试

Java 集成要点：
- 定义清晰的 schema（JSON Schema）。
- 在接收到函数调用后，进行必要的验证和权限检查再执行。
- 对错误和副作用进行幂等处理与重试策略。

示例流程：Prompt -> LLM 返回函数调用 -> 验证参数 -> 执行工具 -> 返回结果给 LLM（或用户）。

---