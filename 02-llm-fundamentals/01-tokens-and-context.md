# Tokens 和 Context 窗口

- 什么是 Token：LLM 处理文本的基本单位（子词/subword）。不同模型的分词器(tokenizer)行为不同，影响 cost 与生成质量。
- Context 窗口：模型在一次调用中可访问的最大 token 数（包含输入和输出）。较大的窗口适合长文档检索与多轮对话。
- 成本与性能：token 数直接影响计费（按 token 计费），并可能影响延迟与内存消耗。

实用建议：
- 估算 prompt 大小，控制系统提示与示例数量。
- 使用压缩/摘要策略减少上下文占用。
- 用 streaming 输出减少首字延迟.

---