# API 集成模式与最佳实践

模式：
- 旁路缓存（Result Cache）：对重复请求缓存响应，节省成本。
- 分层路由（Router）：根据任务复杂度/成本路由到不同模型。
- 批量与异步调用：对批量任务采用异步/批处理以提高吞吐。

安全与可靠性：
- 密钥管理/轮换、限流、熔断、重试（带指数退避）。
- 日志审计：记录 prompt 与模型响应的元数据（注意隐私合规）。

监控指标：请求成功率、延迟分位、cost per request、token 消耗。

---